[pytest]
# Only the tests/ package contains real pytest cases. test_pipeline.py and
# check_pipeline.py at the repo root are manual diagnostic scripts (their
# functions take arguments, so pytest would mis-collect them as fixtures).
# Scoping collection here keeps a bare `pytest` green for contributors.
testpaths = tests
